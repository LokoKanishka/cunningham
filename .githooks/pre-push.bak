#!/usr/bin/env bash
set -euo pipefail

branch="$(git rev-parse --abbrev-ref HEAD)"
case "$branch" in
  main|master)
    echo "ERROR: push bloqueado desde rama '$branch'. Usá una rama-color (amarillo/verde/naranja/azul) o abrí un PR."
    exit 1
    ;;
esac

exit 0
